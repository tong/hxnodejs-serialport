language: haxe

sudo: false

haxe:
  - "3.4.7"
  - "4.0.3"
  - development

matrix:
  allow_failures:
    - haxe: development

install:
  - haxelib install hxnodejs
  - haxelib dev hxnodejs-serialport .

script:
  - haxe doc.hxml
  - haxe --cwd example/ build.hxml
